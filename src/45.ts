using System;
using System.Linq;

class Program {
    static void Main() {
        // Example usage
        int[] numbers = {1, 2, 3, 4};
        Console.WriteLine(String.Join(", ", numbers));
    }
}
